apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: smart-security
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: ghcr.io/your-org/smart-security/api-gateway:latest
          ports:
            - containerPort: 4000
          envFrom:
            - configMapRef:
                name: platform-config
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
          livenessProbe:
            httpGet:
              path: /health
              port: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: smart-security
spec:
  selector:
    app: api-gateway
  ports:
    - port: 80
      targetPort: 4000
